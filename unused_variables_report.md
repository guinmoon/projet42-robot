# Отчет о неиспользуемых переменных и #define

Анализ проведен для файлов в папках `src` и `include`.

## 📁 include/global_def.h

### Неиспользуемые #define:

1. **`WIRE`** (строка 1)
   - Нигде не используется в коде
   - **Рекомендация**: Удалить

2. **`EYEBORDER`** (строка 7)
   - Используется только в `display.cpp` для расчета высоты спрайта
   - **Статус**: Используется ✅

3. **`AUDIO_ON`, `MOVE_ON`, `SLEEP_ON`, `WIFI_ON`, `DEEP_SLEEP_ON`** (строки 32-36)
   - Закомментированы, не используются
   - **Рекомендация**: Удалить или раскомментировать если нужны

---

## 📁 include/display.h

### Неиспользуемые переменные класса `DisplayHelper`:

1. **`gifFile`** (строка 27)
   - Закомментировано
   - **Рекомендация**: Удалить

2. **`usTemp`** (строка 28)
   - Объявлена как `static`, но не используется в активном коде
   - Используется только в закомментированных методах GIF
   - **Рекомендация**: Удалить или использовать если нужна для GIF

3. **`gifData`** (строка 29)
   - Инициализируется `nullptr`, но не используется (GIF функционал закомментирован)
   - **Рекомендация**: Удалить вместе с GIF кодом

4. **`gifSize`** (строка 30)
   - Инициализируется `0`, но не используется
   - **Рекомендация**: Удалить вместе с GIF кодом

5. **`playGif`** (строка 32)
   - Инициализируется `true`, но не используется
   - **Рекомендация**: Удалить вместе с GIF кодом

6. **`wake`** (строка 33)
   - Используется в методах `stopSleepAnimation()` и `showSleepAnimation()`
   - Но эти методы не вызываются нигде кроме объявления
   - **Статус**: Используется, но сомнительно ✅

7. **`pTurboBuffer`** (строка 35)
   - Не инициализируется и не используется (GIF функционал закомментирован)
   - **Рекомендация**: Удалить

8. **`pFrameBuffer`** (строка 36)
   - Не инициализируется и не используется (GIF функционал закомментирован)
   - **Рекомендация**: Удалить

9. **`batteryHelper`** (строка 38)
   - Закомментировано
   - **Рекомендация**: Удалить

10. **`xOffset`** (строка 40)
    - Объявлена как `static`, инициализируется `0` в `display.cpp`
    - Не используется в активном коде
    - **Рекомендация**: Удалить

11. **`yOffset`** (строка 41)
    - Объявлена как `static`, инициализируется `0` в `display.cpp`
    - Не используется в активном коде
    - **Рекомендация**: Удалить

12. **`matrixEffectInited`** (строка 43)
    - Объявлена, но никогда не устанавливается и не проверяется
    - **Рекомендация**: Удалить

13. **`weatherType`** (строка 47)
    - Объявлена в public, но нигде не используется
    - **Рекомендация**: Удалить

14. **`GifPlayTime`** (строка 51)
    - Используется только в закомментированном коде
    - **Рекомендация**: Удалить

### Неиспользуемые методы:

1. **`TFTDraw`** (строка 64)
   - Объявлен как `static`, но не определен и не используется
   - **Рекомендация**: Удалить

2. **`GIFDraw`**, **`GIFAlloc`**, **`GIFDraw_24bit`** (строки 65-67)
   - Закомментированы
   - **Рекомендация**: Удалить

3. **`loadGIFToMemory`** (строка 68)
   - Определен в `display.cpp`, но закомментирован
   - **Рекомендация**: Удалить

4. **`PlayInfiniteThread`**, **`PlayInfiniteTask`** (строки 69-70)
   - Определены в `display.cpp`, но закомментированы
   - **Рекомендация**: Удалить

5. **`PlayGif`** (строка 74)
   - Определен в `display.cpp`, но закомментирован
   - **Рекомендация**: Удалить

6. **`setVoltageBuf`** (строка 77)
   - Объявлен, но не определен
   - **Рекомендация**: Удалить

7. **`StopGif`** (строка 79)
   - Определен в `display.cpp`, но закомментирован
   - **Рекомендация**: Удалить

8. **`drawBatteryheart`** (строка 80)
   - Объявлен как `static`, но не определен
   - **Рекомендация**: Удалить

9. **`LookDown`**, **`Borring`** (строки 57-58)
   - Объявлены, но не определены
   - **Рекомендация**: Удалить или реализовать

---

## 📁 include/proj42.hpp

### Неиспользуемые переменные класса `Proj42`:

1. **`topTouchCount`** (строка 37)
   - Объявлена, но нигде не используется
   - Есть `touchTopCount` в `Proj42Events`
   - **Рекомендация**: Удалить (дублирует функционал)

2. **Закомментированные переменные** (строки 45-53):
   - `gyroHelper`, `audioHelper`, `luluCharacter`, `batteryHelper`, `touchHelper`, `lvglHelper`, `jsRunner`, `fsWebServer`, `configHelper`, `dogEvents`, `i2cSlaveHelper`
   - **Рекомендация**: Удалить если не планируются к использованию

3. **`lastAction`** (строка 55)
   - Закомментировано
   - **Рекомендация**: Удалить

### Неиспользуемые методы:

1. **`setVoltageBuf`**, **`PauseDog`**, **`ResumeDog`**, **`ShowMenu`**, **`Action1`**, **`Action2`**, **`Action3`**, **`ExitMenu`**, **`DoubleTapCallBack`**, **`LongPressCallBack`** (строки 58-68)
   - Закомментированы
   - **Рекомендация**: Удалить

---

## 📁 include/events.hpp

### Неиспользуемые переменные:

1. **`touchTopCount`**, **`touchTopLastT`**, **`lastAttnT`** (строки 22-24)
   - **Статус**: Используются ✅

### Все переменные используются

---

## 📁 include/sensor_handler.hpp

### Все переменные используются в логике датчиков ✅

---

## 📁 include/servos.hpp

### Неиспользуемые переменные:

1. **`lastUpdateTime`** (строка 19)
   - Объявлена, но не используется (логика времени закомментирована в `updateServos()`)
   - **Рекомендация**: Удалить или раскомментировать использование

### Закомментированные переменные:

1. **`servo_left_back`** (строка 14)
   - Закомментировано
   - **Рекомендация**: Удалить если не используется

---

## 📁 include/WebServerManager.h

### Неиспользуемые переменные:

1. **`weatherStr`** (строка 37)
   - Объявлена, но нигде не используется
   - **Рекомендация**: Удалить

2. **`attemptAutoConnect()`** (строка 51)
   - Объявлен в private, но есть `tryAutoConnect()` в public
   - **Рекомендация**: Удалить дубликат

---

## 📁 include/StorageManager.h

### Все переменные используются ✅

---

## 📁 src/display.cpp

### Неиспользуемые глобальные переменные:

1. **`AnimatedGIF DisplayHelper::gif`** (строка 17)
   - Закомментировано
   - **Рекомендация**: Удалить

2. **`usTemp`** (строка 24)
   - Объявлена, но не используется в активном коде
   - **Рекомендация**: Удалить

3. **`xOffset`, `yOffset`** (строки 26-27)
   - Инициализируются `0`, но не используются
   - **Рекомендация**: Удалить

### Закомментированный код:

- Весь блок GIF функционала (строки 17, 108-280)
- **Рекомендация**: Удалить полностью если не нужен

---

## 📁 src/events.cpp

### Все переменные используются ✅

---

## 📁 src/sensor_handler.cpp

### Все переменные используются ✅

---

## 📁 src/servos.cpp

### Неиспользуемые переменные:

1. **`lastUpdateTime`**
   - Объявлена в классе, но не используется (логика закомментирована)
   - **Рекомендация**: Удалить

### Закомментированные сервоприводы:

- `servo_left_back`, `servo_right_back`, `servo_right_front`, `servo_tail`
- **Рекомендация**: Удалить если не используются

---

## 📁 src/WebServerManager.cpp

### Неиспользуемые переменные:

1. **`i`** в методе `WebServerTask()` (строка 22)
   - Используется только для счетчика вызовов `getCurrentTime()`
   - **Статус**: Используется, но можно оптимизировать ✅

### Все остальные переменные используются ✅

---

## 📁 src/StorageManager.cpp

### Все переменные используются ✅

---

## 📁 src/wifi_config.cpp

### Все переменные используются ✅

---

## 📁 src/main.cpp

### Все переменные используются ✅

---

## 📁 src/proj42.cpp

### Закомментированный код:

1. **Строки 7-10, 13-16**: Закомментированные инициализации helper-ов
2. **Строки 45-50, 67-80**: Закомментированные вызовы методов в `Init()`
   - **Рекомендация**: Удалить или раскомментировать если нужны

---

## 🎯 Приоритетные действия:

### 🔴 Высокий приоритет (удалить):

1. **`include/global_def.h`**: Удалить `WIRE` и закомментированные #define
2. **`include/display.h`**: Удалить все GIF-related переменные и методы
3. **`include/display.h`**: Удалить `weatherType`, `xOffset`, `yOffset`, `matrixEffectInited`
4. **`include/proj42.hpp`**: Удалить `topTouchCount`
5. **`include/WebServerManager.h`**: Удалить `weatherStr`
6. **`src/display.cpp`**: Удалить весь закомментированный GIF код
7. **`include/servos.hpp`**: Удалить `lastUpdateTime`

### 🟡 Средний приоритет (решить нужно ли):

1. Методы `stopSleepAnimation()`, `showSleepAnimation()` - используются?
2. Закомментированные helper-ы в `Proj42` - будут использоваться?
3. Закомментированные сервоприводы - будут добавлены?

### 🟢 Низкий приоритет:

1. Очистка закомментированного кода в `proj42.cpp::Init()`

---

## 📊 Сводная статистика:

| Файл | Неиспользуемые переменные | Неиспользуемые методы | #define |
|------|---------------------------|------------------------|---------|
| global_def.h | 0 | 0 | 6 |
| display.h | 14 | 12 | 0 |
| proj42.hpp | 13 | 10 | 0 |
| WebServerManager.h | 2 | 1 | 0 |
| servos.hpp | 1 | 0 | 0 |
| display.cpp | 4 | 0 | 0 |

**Итого**: ~34 неиспользуемые переменные, ~23 неиспользуемых метода, ~6 неиспользуемых #define
